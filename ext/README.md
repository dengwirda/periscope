
Python bindings for Akima's cubic interpolants.

    python -m numpy.f2py -c -m akima_ *.f

    requires: 
    sudo apt-get install gfortran
    sudo apt-get install ninja-build
    pip install numpy meson

